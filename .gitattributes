# Normalise line endings to LF in the repo; checkout as-is on all platforms
* text=auto eol=lf

# Force LF for shell scripts (must be LF for Linux/Docker execution)
*.sh text eol=lf

# Binary assets — do not touch line endings
*.svg binary
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
