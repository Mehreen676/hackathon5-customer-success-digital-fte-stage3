# ============================================================
# Nexora Customer Success Digital FTE — Stage 3 Dependencies
# Hackathon 5 | Author: Mehreen Asghar
# ============================================================

# ── Web Framework ───────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Data Validation ─────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Database ────────────────────────────────────────────────
sqlalchemy>=2.0.48           # ORM with PostgreSQL-ready declarative syntax
aiosqlite==0.20.0            # Async SQLite driver (for async test support)
# psycopg2-binary==2.9.9    # Uncomment for PostgreSQL production deployment

# ── HTTP Client ─────────────────────────────────────────────
httpx==0.27.0                # Async HTTP client — used by FastAPI TestClient
requests==2.32.3             # Sync HTTP for channel handlers

# ── Environment ─────────────────────────────────────────────
python-dotenv==1.0.1         # .env file loading

# ── Form data ───────────────────────────────────────────────
python-multipart==0.0.9      # Required for FastAPI form parsing

# ── AI / LLM Providers (Stage 3) ────────────────────────────
# Install the provider(s) matching your LLM_PROVIDER env var.
# At least one is required for AI response generation.
# Without these, the system gracefully falls back to rule-based responses.

anthropic>=0.28.0            # Anthropic Claude (default provider)
# openai>=1.30.1             # OpenAI GPT-4o / GPT-4o-mini
# google-generativeai>=0.7.0 # Google Gemini

# ── Testing ─────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# ── Stage 4 (real channel integrations — not yet active) ────
# twilio>=9.0.0              # WhatsApp Business API
# google-auth>=2.0.0         # Gmail API authentication
# google-api-python-client   # Gmail API client
